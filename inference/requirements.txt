fastapi==0.115.0
uvicorn[standard]==0.30.6
transformers==4.44.2
accelerate==0.34.2
easyocr==1.7.1
pillow==10.4.0
numpy==1.26.4
python-multipart==0.0.9
# torch/torchvision are installed separately in the Dockerfile so the correct
# CUDA (GPU) or CPU wheel is used depending on the base image.
